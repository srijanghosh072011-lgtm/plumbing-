# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Build / dependency output (none today, but keep the repo clean if added later)
node_modules/
dist/
.cache/

# Local environment / secrets — never commit these
.env
.env.*
*.pem
*.key

# Archives that occasionally get dropped into the working tree
*.zip
